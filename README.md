
where:

- `nCustomers` = number of customers
- `nFacilities` = number of potential facilities
- `capacity` = facility capacity parameter
- `instanceID` = identifier of the random instance

---

## 📄 Format of the `.dat` files

Each file is written in plain text format and can be directly used
within optimization models implemented in Mosel/Xpress.

The structure includes the data defining the facility location instance,
such as problem dimensions, costs, capacities, and preference-related
parameters as described in the article.

---

## 📝 Citation

If you use these instances, please cite the corresponding article:

